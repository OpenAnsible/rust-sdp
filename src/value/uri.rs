// extern crate regex;
extern crate url;

use std::fmt;
use std::str::FromStr;
use std::string::ToString;

// use regex::Regex;
pub use self::url::{ Url, ParseError };


// impl FromStr for Url {
//  fn from_str(s: &str) -> Result<Url, ParseError> {
//      Url::parse(s)
//  }
// }

